import 'package:flutter/material.dart';

class PersonalUpdates extends StatelessWidget {
  const PersonalUpdates({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(),
      ),
    );
  }
}
